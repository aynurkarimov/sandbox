const mwNotFound = require('./notFound');
const mwLogger = require('./logger');
const mwErrorHandler = require('./errorHandler');
const mwHeaders = require('./headers');

module.exports = {
  mwNotFound,
  mwLogger,
  mwErrorHandler,
  mwHeaders
}